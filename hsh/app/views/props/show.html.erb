<h1>Propiedad <%= @prop.nombre%></h1>
<h3><%= @prop.ubicacion %></h3>

<% @prop.images.each do |i| %>
	<%= image_tag i %>
	<br>
<% end %>

<%= link_to "Editar propiedad", edit_prop_path, class: 'btn btn-info btn-sm' %>
